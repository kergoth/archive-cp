#!/bin/bash

set -euo pipefail

scriptdir="$(cd "$(dirname "$0")" && pwd -P)"
cd "$(dirname "$scriptdir")"

if ! command -v uv &>/dev/null; then
    echo >&2 "Error: uv not found. Install from https://docs.astral.sh/uv/getting-started/installation/"
    exit 1
fi

uv sync
